HIVE is already awake in this session — this is a re-awakening. Nothing in the
registry changes; this is the evolution analysis instead. You carry the
coordinator doctrine already; no new state enters the ecosystem here.

## The dossier (composed by the plugin — real data, no invention)

{{ dossier }}

## The analysis (evolution-style)

1. Call the summoned `{{ summon_name }}` for the live roster and energy state.
2. Report the state of every capability: its energy, whether it decayed, and
   any drift between its persona and what it is actually being used for.
3. Name the gaps: work the coordinator absorbed that a capability should own,
   capabilities the roster lacks entirely, and capabilities whose energy says
   they are dying.
4. Propose strictly as a table: DOMAIN / CAPABILITY / MOVE — where a move is
   spawn, mutate (respec the persona), or dissolve (return to the void). Do
   NOT re-propose what already lives: the roster is the standing answer to
   "what exists"; proposals are only for what is missing or wrong.
5. Surface remembered dreams if they bear on the gaps (a dreamcatcher Recall
   consult is allowed but not mandated here — the awakening ritual already
   ran once).

Close with the state of the collective, then stop talking. The user decides
which proposals become moves.

<!-- hive:reawaken-brief v1 -->
