{
  "name": "dsh-berget-usage",
  "version": "0.2.0",
  "private": true,
  "description": "Berget provider plugin for the shared usage panel: a 'Berget' tab (rolling budget windows, per-model breakdown and caps, subscription period and API credits) fed by its own /api/berget-usage/snapshot route. Standalone-panel fallback when dsh-provider-usage is absent.",
  "type": "module",
  "main": "./index.js",
  "exports": {
    ".": "./index.js",
    "./client": "./client.js",
    "./package.json": "./package.json"
  },
  "files": [
    "index.js",
    "client.js",
    "cordis.patch.yml"
  ],
  "scripts": {
    "test": "node --test"
  },
  "dsh": {
    "client": {
      "platform": "web",
      "inject": [],
      "external": []
    },
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  }
}
