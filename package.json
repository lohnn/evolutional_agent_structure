{
  "name": "hive-board",
  "version": "0.1.0",
  "description": "hive-board — kanban + mission-control viewer over HIVE's on-disk state. Phase 1: read-only viewer.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "evolutional-agent-structure": "file:../evolutional_agent_structure",
    "@opencode-ai/sdk": "^1.15.7"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.11.0",
    "@types/bun": "^1.3.0",
    "happy-dom": "^20.11.0",
    "typescript": "^6.0.3"
  }
}
